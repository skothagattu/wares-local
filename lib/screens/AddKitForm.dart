import 'package:flutter/cupertino.dart';

class AddKitForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Add Kit"),
    );
  }
}
