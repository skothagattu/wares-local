import 'package:flutter/cupertino.dart';

class UpdateKitForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Update Kit"),
    );
  }
}
